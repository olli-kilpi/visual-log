---
// Generic iframe embed for other content types
interface Props {
  src: string;
}

const { src } = Astro.props;
---

<iframe
  class="h-full w-full border-0"
  src={src}
  allowfullscreen
  loading="lazy"
  sandbox="allow-scripts allow-same-origin allow-forms"
></iframe>
